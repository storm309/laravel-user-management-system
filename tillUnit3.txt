// request lifecycle in laravel

Request(User or View)
 ↓
index.php
 ↓
Bootstrap
 ↓
Kernel
 ↓
Middleware
 ↓
Route
 ↓
Controller
 ↓
Model / View
 ↓
Response (User or View)


// Important Commands
Q1. Create a new Laravel project named "myapp".
Command: composer create-project --prefer-dist laravel/laravel myapp
Q2. Create a new controller named "ProductController".
Command: php artisan make:controller ProductController
Q3. Create a new model named "Product".
Command: php artisan make:model Product
Q4. Run the Laravel development server.
Command: php artisan serve
Q5. Create a resource controller named "UserController".
Command: php artisan make:controller UserController --resource
Q6. Create a Api controller named "ApiController".
Command: php artisan make:controller ApiController --api
Q7. Create a Single Action controller named "SingleActionController".
Command: php artisan make:controller SingleActionController --invokable
Q8. use app.php is a model and create a resource controller named "AppController".
Command: php artisan make:controller AppController --resource --model=App

// Unit 3: Resourceful Routing in Laravel
Resourceful routing in Laravel provides a convenient way to create routes that handle typical CRUD (Create, Read, Update, Delete) operations for a resource.
By using the `Route::resource` method, you can automatically generate all the necessary routes for a controller that follows the standard naming conventions.
Here’s how you can set up resourceful routing for a `ProductController` in Laravel:
1. First, ensure you have a `ProductController` created. If you haven't created it yet, you can do so using the following command:
   ```bash
   php artisan make:controller ProductController --resource
   ```
   This command will create a controller with methods for all the standard CRUD operations: `index`, `create`, `store`, `show`, `edit`, `update`, and `destroy`.
2. Next, you need to define the resourceful routes in your `routes/web.php` file. You can do this by adding the following line:
   ```php
   use App\Http\Controllers\ProductController;
    Route::resource('products', ProductController::class);
    ```
    This will generate the following routes:
| HTTP Method | URI                | Action       | Route Name         |
|-------------|--------------------|--------------|--------------------|
| GET         | /products          | index        | products.index     |
| GET         | /products/create   | create       | products.create    |
| POST        | /products          | store        | products.store     |
| GET         | /products/{id}     | show         | products.show      |
| GET         | /products/{id}/edit| edit         | products.edit      |
| PUT/PATCH   | /products/{id}     | update       | products.update    |
| DELETE      | /products/{id}     | destroy      | products.destroy   |


## Middleware in Laravel
Middleware in Laravel is a mechanism for filtering HTTP requests entering your application. It acts as a bridge between a request and a response, allowing you to perform various tasks such as authentication, logging, and modifying request and response objects.
### Common Middleware in Laravel:
command: php artisan make:middleware CheckAge
1. **Authentication Middleware**: This middleware checks if the user is authenticated before allowing access to certain routes. It is typically used to protect routes that require user login.
2. **CSRF Protection Middleware**: This middleware protects your application from cross-site request forgery attacks by generating a token for each active user session. It ensures that the incoming requests are from authenticated users.
3. **Logging Middleware**: This middleware can be used to log details about incoming requests and outgoing responses, which can be helpful for debugging and monitoring your application.
4. **Custom Middleware**: You can create your own middleware to perform specific tasks based on your application's requirements. For example, you might create a middleware to check if a user has a certain role before allowing access to a route.
### Creating Custom Middleware:
To create a custom middleware, you can use the Artisan command:
```bashphp artisan make:middleware CheclUser
```
This will create a new middleware class in the `app/Http/Middleware` directory. You can then define the logic for your middleware in the `handle` method. For example, if you want to check if a user is old enough to access a certain route, you can implement the logic
as follows:
```php<?php
namespace App\Http\Middleware;\
use Closure;
use Illuminate\Http\Request;

// Middleware types - Global Middleware: Applied to all routes in the application.
// Route Middleware: Applied to specific routes or groups of routes.
// Middleware Parameters - You can pass parameters to middleware to customize their behavior. For example,
// you might have a middleware that checks for a specific user role, and you can pass the required role as a parameter.


// Blade Templating in Laravel
Blade is Laravel's powerful templating engine that allows you to create dynamic and reusable views. It provides a simple syntax for defining templates and includes features such as template inheritance, sections, and components.
### Blade Syntax:
1. **Template Inheritance**: Blade allows you to define a base layout and then extend it in your child views. This promotes code reusability and helps maintain a consistent design across your application.
   ```php
   <!-- resources/views/layouts/app.blade.php -->
   <html>
   <head>
       <title>My Application</title>
   </head>
   <body>
       @yield('content')
   </body>
   </html>
   ```
   ```php
   <!-- resources/views/home.blade.php -->
   @extends('layouts.app')

   @section('content')
       <h1>Welcome to My Application</h1>
       <p>This is the home page.</p>
   @endsection
   ```
2. **Blade Directives**: Blade provides various directives for control structures, loops, and other common tasks. For example:
   ```php
   @if($user->isAdmin())
       <p>Welcome, Admin!</p>
   @else
       <p>Welcome, User!</p>
   @endif
   ```
   ```php
   @foreach($products as $product)
       <p>{{ $product->name }}</p>
   @endforeach
   ```
3. **Blade Components**: Blade allows you to create reusable components that can be included in multiple views. This is useful for creating common UI elements such as headers, footers, and navigation bars.
   ```php
   <!-- resources/views/components/header.blade.php -->
    <header>
       <h1>My Application Header</h1>
   </header>
   ```
   ```php
   <!-- resources/views/home.blade.php -->
   @extends('layouts.app')
    @section('content')
         @include('components.header')
         <h1>Welcome to My Application</h1>
         <p>This is the home page.</p>
    @endsection
   ```

// Controlled Statement in Laravel
1. **If Statement**: The `@if` directive is used to execute a block of code if a specified condition is true.
   ```php
   @if($user->isAdmin())
       <p>Welcome, Admin!</p>
   @else
       <p>Welcome, User!</p>
   @endif
   ```
2. **Switch Statement**: The `@switch` directive is used to execute different blocks of code based on the value of a variable.
   ```php
   @switch($user->role)
       @case('admin')
           <p>Welcome, Admin!</p>
           @break
       @case('editor')
           <p>Welcome, Editor!</p>
           @break
       @default
           <p>Welcome, User!</p>
   @endswitch
   ```
3. **For Loop**: The `@for` directive is used to execute a block of code a specified number of times.
   ```php
   @for($i = 0; $i < 5; $i++)
       <p>Iteration {{ $i }}</p>
   @endfor
   ```
4. **Foreach Loop**: The `@foreach` directive is used to iterate over an array or collection.
   ```php
   @foreach($products as $product)
       <p>{{ $product->name }}</p>
   @endforeach
   ```
5. **While Loop**: The `@while` directive is used to execute a block of code as long as a specified condition is true.
   ```php
   @while($count < 10)
       <p>Count: {{ $count }}</p>
       <?php $count++; ?>
   @endwhile
   ```
6. Unless Statement: The `@unless` directive is used to execute a block of code if a specified condition is false.
   ```php
   @unless($user->isAdmin())
       <p>Welcome, User!</p>
   @endunless
   ```
7. **Isset Statement**: The `@isset` directive is used to check if a variable is set and is not null.
   ```php
   @isset($user)
       <p>User is set.</p>
   @endisset
   ```
8. **Empty Statement**: The `@empty` directive is used to check if a variable is empty.
   ```php
   @empty($products)
       <p>No products available.</p>
   @endempty
   ```

// breeze is a package that provides authentication scaffolding for Laravel applications. It offers a simple and elegant way to implement user authentication, including registration, login, password reset, and email verification.
To install Laravel Breeze, you can use the following command:
```bash
composer require laravel/breeze --dev
```
After installing the package, you can run the Breeze installation command to set up the authentication scaffolding:
```bash
php artisan breeze:install
```
This command will publish the necessary views, routes, controllers, and other files required for authentication. After running the installation command, you can run the following command to compile the frontend assets:
```bash
npm artisan migrate
and npm install && npm run dev
```
This will install the required npm packages and compile the frontend assets using Laravel Mix.
Once you have completed these steps, you can access the authentication features of your Laravel application by visiting the appropriate routes
(e.g., `/register`, `/login`, etc.). Breeze provides a clean and simple user interface for authentication, making it easy to get
started with user authentication in your Laravel application.
